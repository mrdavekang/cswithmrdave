const app=document.querySelector('#app');
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const HUB='https://beetlebit-hub.cytron.io/chapter-resources/beetlebit-setup-guide';
const inventory=['Top and lower chassis plates','REKA:BIT and micro:bit','2 motors, brackets and wheels','Castor wheel','Ultrasonic sensor and bracket','Maker Line sensor','Servo, horn and gripper pieces','Battery holder and 4 AA batteries','Sensor cables and Velcro dots','Bolts, nuts, spacers and stands','Phillips screwdrivers','Cross wrench'];
const cards=[{id:'mission',stage:0,title:'Your mission: build it right.'},{id:'parts',stage:0,title:'Meet your robot’s parts.'},{id:'inventory',stage:0,title:'Check your own set.'},{id:'safety',stage:1,title:'Open carefully. Work safely.'},{id:'safety2',stage:1,title:'Keep the power off.'},{id:'quiz1',stage:2,title:'A wire is in the wrong place.'},{id:'quiz2',stage:2,title:'The bolt will not fit.'},{id:'quiz3',stage:2,title:'Ready to begin?'},{id:'prepare',stage:3,title:'Make a calm build bench.'},{id:'diagram',stage:3,title:'Read before you build.'},...STEPS.map(s=>({id:'step'+s.n,stage:4,title:s.title,step:s})),{id:'inspect',stage:5,title:'Inspect before you connect.'},{id:'test',stage:5,title:'First power. Short test.'},{id:'pack',stage:5,title:'Stop safely. Save your build.'},{id:'reflect',stage:6,title:'What changed in your thinking?'}];
let state=null,teacher=false,storageOK=true;
function notify(m){$('#notice').textContent=m;clearTimeout(notify.timer);notify.timer=setTimeout(()=>$('#notice').textContent='',6500)}
function key(){return 'beetlebit-w4:'+state.name.toLowerCase()+':'+state.className.toLowerCase()}
function save(){if(teacher||!state)return;try{localStorage.setItem(key(),JSON.stringify(state))}catch{storageOK=false;notify('This browser cannot save progress. Use Save backup before leaving.')}}
function fresh(p){return {version:1,...p,at:0,furthest:0,checked:{},items:{},answers:{},reflection:{}}}
function valid(s){return s&&s.version===1&&typeof s.name==='string'&&s.name.trim()&&s.name.length<=40&&typeof s.className==='string'&&s.className.length<=30&&['individual','partner'].includes(s.team)&&['en','bm'].includes(s.language)&&Number.isInteger(s.at)&&s.at>=0&&s.at<cards.length&&Number.isInteger(s.furthest)&&s.furthest>=0&&s.furthest<cards.length&&['checked','items','answers','reflection'].every(k=>s[k]&&typeof s[k]==='object'&&!Array.isArray(s[k]))}
function clean(s){const r=fresh({name:s.name.trim(),className:s.className.trim(),team:s.team,language:s.language});r.at=s.at;r.furthest=Math.max(s.at,s.furthest);for(const [k,v] of Object.entries(s.checked)){if(/^(step\d+|safe\d|prep\d|inspect\d|teacher|power|pack)$/.test(k)&&v===true)r.checked[k]=true}inventory.forEach((_,i)=>{if(['found','missing'].includes(s.items[i]))r.items[i]=s.items[i]});['quiz1','quiz2','quiz3'].forEach(k=>{if(Number.isInteger(s.answers[k])&&s.answers[k]>=0&&s.answers[k]<3)r.answers[k]=s.answers[k]});['learned','evidence','next'].forEach(k=>{if(typeof s.reflection[k]==='string')r.reflection[k]=s.reflection[k].slice(0,1500)});return r}
function landing(){state=null;teacher=false;$('#identity').textContent='Your first robot build';$('#save').hidden=true;$('#exit').hidden=true;$('#progress').hidden=true;app.innerHTML=`<section class="card entry"><div><span class="tag">WEEK 04 • FIRST BUILD</span><h2>Welcome to the<br>build bench.</h2><p>Today, we turn a box of parts into a carefully checked Beetle:bit.</p><form id="signin"><label class="field">Your first name<input name="name" required maxlength="40" autocomplete="given-name" type="text"></label><label class="field">Your class<input name="className" required maxlength="30" placeholder="e.g. Year 6" type="text"></label><label class="field">How are you working?<select name="team"><option value="individual">On my own</option><option value="partner">With a partner</option></select></label><label class="field">Language support<select name="language"><option value="en">English</option><option value="bm">English + Bahasa Melayu key words</option></select></label><button class="primary" type="submit">Start or resume →</button></form><p class="muted">Your name and progress stay in this browser—not sent to a teacher. Use a backup when changing devices.</p><button class="text-button" id="restore">Restore a local backup</button></div><div class="entry-art"><span class="eyebrow">DESIGN • ASSEMBLE • CHECK</span><img src="assets/robot.png" alt="Beetle:bit robot with micro:bit controller and front gripper"><strong>A good builder checks<br>before powering up.</strong><p>One card. One build step.<br>Take your time. Get it right.</p></div></section>`;
$('#signin').elements.name.oninput=e=>{$('#signin').elements.className.required=e.target.value.trim().toLowerCase()!=='teacher'};
$('#signin').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),p={name:String(f.get('name')).trim(),className:String(f.get('className')).trim(),team:f.get('team'),language:f.get('language')};teacher=p.name.toLowerCase()==='teacher';if(!p.name||(!teacher&&!p.className)){notify('Enter your first name and class.');return}state=fresh(p);if(!teacher){try{const old=JSON.parse(localStorage.getItem(key())||'null');if(valid(old)){state=clean(old);state.team=p.team;state.language=p.language}}catch{storageOK=false}}save();render()};$('#restore').onclick=()=>$('#restore-file').click()}
function img(page,alt,crop){const regions=[[.08,.15,.34,.505],[.37,.15,.63,.505],[.655,.15,.92,.505],[.08,.15,.63,.505],[.37,.15,.92,.505],[0,0,1,1]],s=crop?STEPS.find(s=>s.n===crop):null,r=s?regions[s.c]:regions[5],width=r[2]-r[0],height=r[3]-r[1],picture=`<img src="assets/page-${String(page).padStart(2,'0')}.webp" alt="${esc(alt)}" ${s&&s.c!==5?`style="width:${100/width}%;left:${-r[0]/width*100}%;top:${-r[1]/height*100}%;"`:''}>`;return `<figure class="diagram">${s&&s.c!==5?`<div class="crop-frame" style="aspect-ratio:${width*1800/(height*1371)}">${picture}</div>`:picture}<figcaption><span>Cytron guide · page ${page}${crop?' · step '+crop:''}</span><button data-image="${page}">View full page ↗</button></figcaption></figure>`}
function check(id,t){return `<label class="check"><input type="checkbox" data-check="${id}" ${state.checked[id]?'checked':''}><span>${t}</span></label>`}
function video(id,title){return `<div class="video-choice"><b>${title}</b><p>Watch the matching part, pause, then return to your build card. Video playback connects to YouTube; if school blocks it, use the booklet pictures.</p><button data-video="${id}">Load video</button> <a href="https://www.youtube.com/watch?v=${id}" target="_blank" rel="noopener">Open on YouTube ↗</a><div class="video" hidden></div></div>`}
const quizzes={quiz1:{q:'You notice a cable needs moving. Your partner reaches for it while the battery is connected. What is your next move?',options:['Move the cable quickly before the wheels turn.','Switch OFF, disconnect power, then ask your teacher to check.','Hold the wheels still while your partner changes the wire.'],right:1,why:'Power off and disconnect before changing any connection. Holding the wheels does not make live wiring safe.'},quiz2:{q:'The diagram says a short bolt, but yours will not turn easily. What should you do?',options:['Push harder with the screwdriver.','Take a bolt from another student’s robot.','Stop. Recheck the size and hole, then ask your teacher.'],right:2,why:'Resistance is a clue to check, not an invitation to force. A wrong bolt can damage the parts.'},quiz3:{q:'Which bench is ready for building?',options:['Parts sorted in a tray, booklet picture open, power disconnected.','Every bag emptied into one pile so everything is visible.','Battery connected so you can test each wire as you add it.'],right:0,why:'Sorting and comparing before acting prevents mistakes. Leave power disconnected until the completed wiring is inspected.'}};
function body(c){if(c.step){const s=c.step;return `<div class="split"><div>${img(s.p,`Booklet diagram for step ${s.n}: ${s.title}`,s.n)}<p class="small muted">Look at the orientation before touching the parts. Enlarge the full page for labels and wiring tables.</p></div><div><div class="materials"><h3>On your bench</h3><p>${s.kit}</p></div><p class="instruction">${s.do}</p>${s.note?`<div class="warning"><b>Before you continue</b><p>${s.note}</p></div>`:''}${check('step'+s.n,'Built and checked: '+s.check)}</div></div>${s.n===1||s.n===21||s.n===31?video('uMzD8UFlGDc','Cytron’s assembly walkthrough'):''}<div class="exit-row"><button class="text-button" id="missing">A part is missing / I need help</button><button class="text-button" id="pause">Pause here and reflect</button></div>`}
switch(c.id){case'mission':return `<div class="split"><div><p class="instruction">Our robot needs to carry and grip objects. Before it can follow our code, its body must be built correctly.</p><h3>By the end, you should be able to…</h3><ol class="stepslist"><li>Identify the controller, motors, sensors and gripper.</li><li>Use diagrams to assemble and check a secure build stage.</li><li>Explain why we inspect before switching on.</li></ol><div class="warning"><b>Build carefully, not quickly.</b><p>You do not need to finish all 36 steps today. A checked checkpoint and a clear next step count as progress.</p></div></div><div><figure class="diagram"><img src="assets/robot.png" alt="Completed Beetle:bit robot showing its wheels, controller, ultrasonic sensor and gripper"><figcaption>The robot you are working towards.</figcaption></figure><p class="small">Today: unpack → prepare → build → inspect → reflect. Only test a complete, teacher-checked robot.</p></div></div>`;
case'parts':return `<div class="split">${img(6,'Labelled kit inventory from the Cytron guide')}<div><p class="instruction">Point to each part in your set. Tell your partner what job you predict it will do.</p><div class="mini-grid"><div><b>Control</b>micro:bit runs the program. REKA:BIT connects the motors and other parts.</div><div><b>Movement</b>DC motors turn the wheels. A servo moves the gripper.</div><div><b>Input</b>Ultrasonic senses distance. Maker Line senses the surface below.</div></div><p class="phase"><b>Explain:</b> Why does the line sensor need to face the floor?</p><p class="muted">Today we fit the sensors. We will learn to program them in later lessons.</p></div></div>`;
case'inventory':return `<p>Check only your own set. Keep small parts in a tray. If anything is missing, mark it and show your teacher.</p><div class="split"><div class="inventory">${inventory.map((n,i)=>`<label>${n}<select data-item="${i}" aria-label="${n}"><option value="">Not checked</option><option value="found" ${state.items[i]==='found'?'selected':''}>Found</option><option value="missing" ${state.items[i]==='missing'?'selected':''}>Missing / unsure</option></select></label>`).join('')}</div><div>${img(7,'Cytron guide hardware and tools inventory')}<div id="missing-list" class="warning">${missingText()}</div><p class="small">Check hardware sizes and quantities again at each build step. Booklet discrepancies are flagged on cards 14 and 24.</p></div></div>`;
case'safety':return `<div class="mini-grid"><div><b>1 / Open over a tray</b>Keep packaging and labels. Open one bag at a time. Ask an adult if packaging needs cutting.</div><div><b>2 / Handle with care</b>Hold circuit boards by the edges. Keep drinks away. Never put small parts in your mouth.</div><div><b>3 / Use tools gently</b>Use the screwdriver that fits. Keep fingers clear of its tip. Snug is enough—never force.</div></div>${check('safe1','My table is clear, small parts have a tray and I know where to put the packaging.')}${check('safe2','I will stop and ask for help when a part does not fit.')}`;
case'safety2':return `<p class="instruction">The robot stays unpowered while we build and connect wires.</p><ol class="stepslist"><li>Keep the battery switch <b>OFF</b> and its plug disconnected.</li><li>Unplug the micro:bit’s USB cable before assembly.</li><li>Keep fingers, hair and loose clothing away from gears and wheels.</li><li>Never force a servo to turn. Ask your teacher about alignment.</li></ol><div class="warning"><b>Stop → switch off → disconnect → ask.</b><p>Use this sequence before correcting wiring. If anything becomes hot or smells unusual, stop and alert your teacher immediately.</p></div>${check('safe3','Battery and USB power are disconnected. I will ask for a teacher check before powering up.')}`;
case'quiz1':case'quiz2':case'quiz3':{const q=quizzes[c.id],a=state.answers[c.id];return `<fieldset class="questions"><legend>${q.q}</legend>${q.options.map((o,i)=>`<button class="quiz-choice" data-answer="${i}" aria-pressed="${a===i}">${String.fromCharCode(65+i)}. ${o}</button>`).join('')}</fieldset><div class="feedback" aria-live="polite">${a===undefined?'Choose an action, then explain your reason.':a===q.right?'✓ Ready. '+q.why:'Not yet. '+q.why+' Try again.'}</div>`}
case'prepare':return `<div class="mini-grid"><div><b>Parts zone</b>Sort chassis pieces, electronics and small fixings. Keep labels beside their groups.</div><div><b>Build zone</b>Leave a clear area for the robot. Put only the parts for this card here.</div><div><b>Check zone</b>Keep the screen beside the build—not underneath parts. Compare before moving on.</div></div><p class="phase instruction">${state.team==='partner'?'Builder handles the parts. Checker reads the card and compares the diagram. Swap roles after every three build cards.':'Read the card aloud, build the step, then become your own checker. Ask a neighbour to inspect each completed stage.'}</p>${check('prep1','My parts are organised and I know who is building and checking.')}`;
case'diagram':return `<div class="split">${img(9,'First assembly page showing orientation, fixing holes and stands')}<div><h3>Look → predict → build → check</h3><ol class="stepslist"><li><b>Look:</b> find the front of the plate and count the holes.</li><li><b>Predict:</b> point to where the four stands will go. Which side will the nuts be on?</li><li><b>Build:</b> gather the listed size and quantity. Do one step.</li><li><b>Check:</b> compare orientation and fixings with the photo.</li></ol><p><b>Size clue:</b> M3 is the thread size; 6 mm is the bolt length. A stand separates two layers; a spacer keeps a joint apart.</p><p class="muted">The cards keep the booklet’s original step numbers. “View full page” shows surrounding diagrams.</p></div></div>${video('uMzD8UFlGDc','Watch Cytron assemble the same robot')}${check('prep2','I can identify the four stand positions in the diagram before I start.')}`;
case'inspect':return `<p class="instruction">Do not power up yet. Put your tools down and inspect the whole robot with your teacher.</p>${check('inspect1','All mounting bolts and nuts are secure; boards are not bent and wheels are clear.')}${check('inspect2','No cable can reach a wheel or gear, and no wire is trapped between the decks.')}${check('inspect3','Motor colours match step 31; servo is on S1; sensor ports match steps 35–36.')}${check('inspect4','The micro:bit version and Maker Line white-wire connection have been checked.')}${check('teacher','My teacher has physically inspected this robot and said it is ready for a powered test.')}<div class="warning"><b>A checkbox is a record—not permission.</b><p>Only tick the last box after your teacher has actually checked your robot. If unfinished, pause here and reflect.</p></div><button class="text-button" id="pause">Not ready? Pause and reflect</button>`;
case'test':return `<div class="split">${img(38,'Cytron motor test page showing M1A, M1B, M2A and M2B buttons and direction arrows')}<div><div class="warning"><b>Teacher-supervised test only</b><p>This is for a fully assembled and physically approved robot. Keep fingers away from the wheels and gripper.</p></div><ol class="stepslist"><li>With power OFF, your teacher checks battery polarity and connects the battery plug.</li><li>Your teacher supports the chassis with the wheels clear, away from the table edge.</li><li>Switch on. Briefly press one REKA:BIT motor-test button at a time: <b>M1A, M1B, M2A, M2B</b>.</li><li>Compare the wheel directions with the red arrows in the full-page guide.</li><li>Switch OFF and disconnect before correcting any wire. No response or wrong direction? Ask your teacher.</li></ol><p class="small">Use the board’s physical test buttons; no new code download is needed for this motor check. Servo calibration is a separate teacher-guided job—not a reason to force the gripper.</p>${check('power','We observed the wheel directions with our teacher and switched the robot OFF afterwards.')}</div></div>${video('lY3kOSvGrps','Optional: Cytron Chapter 2 movement resource')}`;
case'pack':return `<p class="instruction">Your next lesson starts with how you leave your robot today.</p><ol class="stepslist"><li>Switch OFF, unplug battery power and disconnect USB.</li><li>Put loose bolts, nuts and tools back in a labelled tray or bag.</li><li>Label your own robot or storage tray as your teacher directs.</li><li>Record the last step you checked. Do not tick unfinished steps.</li><li>Save a backup if you may use another device next time.</li></ol>${check('pack','My robot is unpowered, my parts are stored and my bench is clear.')}`;
case'reflect':return `<div class="mini-grid"><div><b>Build progress</b><span class="complete">${done()}</span> / 36 steps checked</div><div><b>My next build step</b>${nextBuild()?`Step ${nextBuild().n}: ${nextBuild().title}`:'Assembly checks recorded. Follow the teacher inspection and test cards.'}</div><div><b>Success today</b>A secure checkpoint, one explained decision and a clear next action.</div></div><label class="field">What did you learn about how a robot is built?<textarea data-reflection="learned" maxlength="1500" placeholder="I used to think… Now I know…">${esc(state.reflection.learned||'')}</textarea></label><label class="field">Give one example of a careful check or a problem you solved.<textarea data-reflection="evidence" maxlength="1500" placeholder="I checked… because…">${esc(state.reflection.evidence||'')}</textarea></label><label class="field">What needs to happen next lesson?<textarea data-reflection="next" maxlength="1500" placeholder="My next step is… I need help with…">${esc(state.reflection.next||'')}</textarea></label><div class="warning"><b>Before you leave</b><p>Power off and disconnect. Store loose parts safely. These notes stay on this device; show them to your teacher.</p></div><button id="return-build">Return to my next build step</button> <button id="backup-reflection" class="primary">Save my progress backup</button>`}}
function done(){return STEPS.filter(s=>state.checked['step'+s.n]).length}function nextBuild(){return STEPS.find(s=>!state.checked['step'+s.n])}function missingText(){const m=inventory.filter((_,i)=>state.items[i]==='missing');return m.length?'<b>Show your teacher:</b><br>'+m.map(esc).join('<br>'):'Check each group. Missing items are not sent automatically to your teacher.'}
function ready(c){if(teacher)return true;if(c.step)return !!state.checked[c.id];if(quizzes[c.id])return state.answers[c.id]===quizzes[c.id].right;const req={safety:['safe1','safe2'],safety2:['safe3'],prepare:['prep1'],diagram:['prep2'],inspect:['inspect1','inspect2','inspect3','inspect4','teacher'],test:['power'],pack:['pack']};if(c.id==='inventory')return inventory.every((_,i)=>['found','missing'].includes(state.items[i]));if(c.id==='inspect'&&done()<36)return false;return(req[c.id]||[]).every(k=>state.checked[k])}
function go(i){if(i<0||i>=cards.length)return;if(cards[i].id==='test'&&!teacher&&!ready(cards.find(c=>c.id==='inspect'))){notify('Complete the assembly and ask for a physical teacher inspection before testing.');i=cards.findIndex(c=>c.id==='inspect')}state.at=i;state.furthest=Math.max(state.furthest,i);save();render();$('#main').focus();window.scrollTo({top:0,behavior:'instant'})}
function render(){const c=cards[state.at];$('#identity').textContent=teacher?'Teacher preview · no progress saved':state.name+' · '+state.className;$('#save').hidden=teacher;$('#exit').hidden=false;$('#progress').hidden=false;$('#progress').innerHTML=`<b class="small">${done()} / 36 build steps checked</b><div class="bar" role="progressbar" aria-label="Build steps checked" aria-valuenow="${done()}" aria-valuemin="0" aria-valuemax="36"><div style="width:${done()/36*100}%"></div></div><span class="small muted">${teacher?'Review any card below.':storageOK?'Saved on this device':'Use a backup: browser saving unavailable'}</span>`;
app.innerHTML=`<nav class="stagebar" aria-label="Lesson stages">${STAGES.map((s,i)=>`<button data-stage="${i}" class="${c.stage===i?'active':''}" ${c.stage===i?'aria-current="step"':''} ${!teacher&&cards.findIndex(x=>x.stage===i)>state.furthest?'disabled':''}>${String(i+1).padStart(2,'0')} ${s}</button>`).join('')}</nav>${teacher?`<label class="jump small">Review card<select id="teacher-jump">${cards.map((x,i)=>`<option value="${i}" ${i===state.at?'selected':''}>${x.step?'Build '+x.step.n:STAGES[x.stage]} · ${x.title}</option>`).join('')}</select></label>`:''}<section class="card"><div class="cardhead"><div><span class="eyebrow">${STAGES[c.stage]}${c.step?' / '+(c.step.n<=6?'Upper deck':c.step.n<=20?'Rolling chassis':c.step.n<=30?'Gripper & body':'Wiring'):''}</span><h2>${c.title}</h2></div><span class="counter">${c.step?'Step '+c.step.n+' / 36':'Card '+(state.at+1)+' / '+cards.length}</span></div>${body(c)}${state.language==='bm'?'<div class="glossary"><b>Key words:</b> bolt = bolt · nut = nat · wire = wayar · wheel = roda · sensor = penderia · check = periksa · switch off = matikan suis</div>':''}<div class="footer"><button id="prev" ${state.at===0?'disabled':''}>← Back</button><p id="next-hint">${state.at===cards.length-1?'Explain one decision to your teacher.':ready(c)?c.step&&state.team==='partner'&&c.step.n%3===0?'Swap builder and checker now.':'One step at a time.':c.id==='inspect'&&done()<36?'Finish the build before a powered test.':'Complete the check before moving on.'}</p><div class="actions">${state.at<cards.length-1?`<button id="next" class="primary" ${!ready(c)?'disabled':''}>${c.step&&c.step.n<36?'Next build step':'Continue'} →</button>`:''}</div></div></section><p class="source">Assembly pictures: Cytron, <i>BEETLE:BIT Quick Start Guide</i>, February 2026. Classroom prompts adapted for this lesson. <a href="${HUB}" target="_blank" rel="noopener">Official setup resources ↗</a></p>`;bind(c)}
function bind(c){$('#prev').onclick=()=>go(state.at-1);if($('#next'))$('#next').onclick=()=>{if(ready(c))go(state.at+1)};document.querySelectorAll('[data-stage]').forEach(b=>b.onclick=()=>go(cards.findIndex(x=>x.stage===Number(b.dataset.stage))));if($('#teacher-jump'))$('#teacher-jump').onchange=e=>go(Number(e.target.value));document.querySelectorAll('[data-check]').forEach(el=>el.onchange=()=>{state.checked[el.dataset.check]=el.checked;save();if($('#next'))$('#next').disabled=!ready(c);$('#next-hint').textContent=ready(c)?'Ready for the next card.':'Complete the check before moving on.';const n=done();$('#progress b').textContent=n+' / 36 build steps checked';$('#progress .bar').setAttribute('aria-valuenow',n);$('#progress .bar>div').style.width=n/36*100+'%'});document.querySelectorAll('[data-item]').forEach(el=>el.onchange=()=>{state.items[el.dataset.item]=el.value;save();$('#missing-list').innerHTML=missingText();$('#next').disabled=!ready(c)});document.querySelectorAll('[data-answer]').forEach(el=>el.onclick=()=>{state.answers[c.id]=Number(el.dataset.answer);save();render()});document.querySelectorAll('[data-reflection]').forEach(el=>el.oninput=()=>{state.reflection[el.dataset.reflection]=el.value;save()});document.querySelectorAll('[data-image]').forEach(el=>el.onclick=()=>{const p=el.dataset.image;$('#full-picture').src=`assets/page-${String(p).padStart(2,'0')}.webp`;$('#full-picture').alt=`Full Cytron guide page ${p}`;$('#picture-caption').textContent='Cytron BEETLE:BIT Quick Start Guide · page '+p;$('#picture').showModal()});document.querySelectorAll('[data-video]').forEach(el=>el.onclick=()=>{const box=el.parentElement.querySelector('.video');box.hidden=false;box.innerHTML=`<iframe src="https://www.youtube-nocookie.com/embed/${el.dataset.video}" title="Cytron Beetlebit video" loading="lazy" allow="encrypted-media; picture-in-picture" allowfullscreen referrerpolicy="strict-origin-when-cross-origin"></iframe>`;el.hidden=true});if($('#pause'))$('#pause').onclick=()=>go(cards.findIndex(x=>x.id==='reflect'));if($('#missing'))$('#missing').onclick=()=>notify('Stop at this step. Keep the parts together and show your teacher this card. Do not take parts from another student’s set.');if($('#return-build'))$('#return-build').onclick=()=>{const s=nextBuild();go(cards.findIndex(x=>x.id===(s?'step'+s.n:'inspect')))};if($('#backup-reflection'))$('#backup-reflection').onclick=backup}
function backup(){if(teacher){notify('Teacher preview does not save learner data.');return}const url=URL.createObjectURL(new Blob([JSON.stringify(state,null,2)],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download='beetlebit-week4-backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);notify('Backup downloaded. It includes your first name, class, checks and reflection.')}
$('#save').onclick=backup;$('#exit').onclick=()=>{save();landing()};$('#close-picture').onclick=()=>$('#picture').close();$('#restore-file').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{if(f.size>100000)throw Error();const s=JSON.parse(await f.text());if(!valid(s)||s.name.trim().toLowerCase()==='teacher')throw Error();if(!confirm('Restore this backup? This replaces saved Week 4 progress for '+s.name+' in '+s.className+' on this device.'))return;state=clean(s);teacher=false;save();render();notify('Your local backup is restored.')}catch{notify('That is not a valid Week 4 backup. Your current progress has not changed.')}finally{e.target.value=''}};
landing();
if(document.modelContext?.registerTool){try{document.modelContext.registerTool({name:'read_beetlebit_lesson_progress',description:'Read the current lesson card and checked-step count. Does not identify the student or change progress.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute(input){if(input&&Object.keys(input).length)throw Error('No parameters accepted');return state?{card:cards[state.at].title,stage:STAGES[cards[state.at].stage],checkedSteps:done(),teacherPreview:teacher}:{stage:'Sign in'}}})}catch{}}
