/* Teacher-created support surrounds unchanged original examination images. */
(() => {
  const escape = text => String(text).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const field = (key, label, rows = 2, hint = '') => `<label class="field"><span>${label}</span>${hint ? `<small>${hint}</small>` : ''}<textarea data-save="${key}" rows="${rows}"></textarea></label>`;
  const select = (key, label, options) => `<label class="field"><span>${label}</span><select data-save="${key}"><option value="">Choose…</option>${options.map(x => `<option>${x}</option>`).join('')}</select></label>`;
  const example = (code, filename = 'practice.py') => `<div class="code-example"><div class="editor-bar"><strong>${filename}</strong><span>Python 3 · read-only</span></div><pre><code>${escape(code)}</code></pre></div>`;
  const editor = (key, label) => `<label class="field code-field"><span>${label}</span><small>Write and run Python in your IDE, then paste the complete subroutine here. You can paste an unfinished draft for feedback.</small><div class="editor-bar"><strong>${key === 'q05_code' ? 'main' : key === 'q06_code' ? 'stretchImage' : 'loadImage'}.py</strong><span>Draft / final source</span></div><div class="editor"><pre class="line-numbers" aria-hidden="true">1</pre><textarea data-save="${key}" rows="16" wrap="off" spellcheck="false" autocapitalize="off" aria-label="${label}"></textarea></div></label>`;
  const official = (asset, label) => `<figure class="official-question"><figcaption><span>Original November 2024 Paper 1 · ${label}</span><button class="button small-button" data-zoom="assets/${asset}">Enlarge</button></figcaption><img src="assets/${asset}" alt="Complete original ${label}; the question-paper PDF in Resources provides selectable text." /></figure>`;
  const uploads = prefix => `<section class="card"><p class="eyebrow">Official test evidence</p><h3>Keep the inputs and results visible</h3><p>Use one or two readable screenshots that match your source code. You may use the second slot for a photograph of a paper trace as additional learning evidence.</p><div class="two-col">${[1,2].map(n => `<div class="upload-slot" data-upload-slot="${prefix}_image${n}" tabindex="0" role="group" aria-label="${prefix} evidence ${n}"><label>Evidence ${n}${n===2?' (optional)':''}<input type="file" accept="image/*" /></label><p class="small">Choose an image, drop it here, or focus this box and paste.</p><img hidden alt="Evidence preview ${n}" /><button class="button small-button remove-upload" hidden type="button">Remove image</button></div>`).join('')}</div></section>`;
  const feedback = (q, codekey) => `<section class="feedback-panel card" data-feedback="${q}"><div class="section-top"><div><p class="eyebrow">Checkpoint · talk about your code</p><h3>Ready for feedback</h3></div><button class="button" data-focus-review="${q}" aria-pressed="false">Focus view</button></div><p>Show these lines to your teacher. Explain your prediction and the result; you do not have to finish the program first.</p><div class="editor-bar"><strong>Relevant lines from your current draft</strong><span>Updates from your source</span></div><pre class="code-excerpt" data-excerpt="${codekey}"></pre><div class="two-col">${select(q+'_focus','I want to check…',['Code placement','The boundary condition','Repetition and re-input','Output or test evidence'])}${field(q+'_question','My question / uncertainty',2)}</div><div class="two-col">${field(q+'_expected','I expected…',2)}${field(q+'_actual','What happened / what I predicted instead…',2)}</div><button class="button primary" data-snapshot="${q}">Save this attempt for feedback</button><p class="small">Keep a dated copy of your draft and answers before editing. Later changes do not replace a saved attempt.</p><details class="attempts"><summary>Saved attempts <span data-attempt-count="${q}">0</span></summary><div data-attempts="${q}"></div></details><div class="feedback-response"><p class="eyebrow">Act on the feedback</p>${field(q+'_advice','The feedback or useful discovery was…',2)}<div class="two-col">${field(q+'_change','The exact change I made…',2)}${field(q+'_retest','I reran this sequence, and the result was…',2)}</div><button class="button" data-retest="${q}">Save correction &amp; retest</button></div></section>`;
  const nav = (next, section) => `<div class="page-actions">${section ? `<button class="button" data-record="${section}">Record this activity</button>` : '<span></span>'}<button class="button primary" data-next="${next}">Continue →</button></div>`;
  const page = (id, number, label, title, tag, body) => ({id, label, html:`<article class="lesson-page" data-page="${id}" hidden><div class="page-heading"><span class="section-number">${number}</span><div><p class="eyebrow">${label}</p><h2>${title}</h2></div><span class="badge">${tag}</span></div>${body}</article>`});
  const teacher = text => `<aside class="teacher-note" hidden>${text}</aside>`;
  const lessons = [
    page('overview','00','Lesson map','Programming with evidence','60 minutes',`
      <section class="card dark"><p class="eyebrow">Your challenge</p><h3>Make a change you can explain and prove.</h3><p>The program loads small greyscale images from text files. Add a dimensions message, then validate the factor that stretches an image horizontally.</p></section>
      <div class="three-col"><section class="card"><p class="eyebrow">Knowledge</p><p>Identify parameters, returned values, integer conversion and loop conditions.</p></section><section class="card"><p class="eyebrow">Skills</p><p>Modify a subroutine, test its behaviour and capture matching evidence.</p></section><section class="card"><p class="eyebrow">Understanding</p><p>Explain why output placement, boundaries and re-input change program behaviour.</p></section></div>
      <section class="card"><h3>What will show your progress?</h3><ol><li>Explain why the dimensions message must follow the returned-value assignment.</li><li>Reject invalid factors repeatedly and continue when a valid factor is entered.</li><li>Use a failed test or feedback to make a change, retest and explain the result.</li></ol><div class="marks-strip"><span>Starter <b>3 marks</b></span><span>Q05 <b>5 marks</b></span><span>Q06 <b>6 marks</b></span><span>Optional Q07 <b>9 marks</b></span></div><p class="small">Official questions retain their original marks. Checkpoints are learning practice. Recording an activity does not award examination marks.</p></section>
      <div class="notice">All activities are available. Follow your teacher's timing. You can export a partial record at any point, including when you need more help.</div>
      ${teacher('<strong>Observation plan</strong><p>Prepare files before teaching. Suggested minutes: starter 0–5; types of learning 5–7; Q05 7–22; Q06 boundary check 22–25; coding, feedback and retest 25–47; pit stop 47–50; plenary 50–55; export 55–60. If teaching starts at 2:00, the 2:30–3:00 visit covers Q06 feedback through export. Adjust to the actual start.</p><p>The Teacher notebook is a manual seven-student checklist on this device. It does not receive students’ work live.</p>')}${nav('resources')}`),
    page('resources','01','Resources','Set up your programming folder','Before you begin',`
      <section class="card"><h3>Use Python 3.10 or newer</h3><p>The skeleton uses <code>match / case</code>. Keep the Python file and text files in the same working folder.</p><ol><li>Download the skeleton and the three named data files below.</li><li>Run the skeleton once in your IDE. Choose <code>1</code>, enter <code>pattern</code>, then choose <code>2</code> to display the loaded image.</li><li>Make separate clean working copies for Q05, Q06 and, if needed, Q07.</li></ol></section>
      <div class="resource-grid">${[['Nov24-Paper1-Preliminary-Material.pdf','Preliminary Material','Read the program scenario'],['Nov24-Paper1-Question-Paper.pdf','Question paper','Official tasks and evidence'],['Paper_1_November_2024_Skeleton.py','Skeleton program','Clean Python source'],['pattern.txt','pattern.txt','Q05 test data'],['one.txt','one.txt','Q06 test data'],['zero.txt','zero.txt','Additional supplied data']].map(([f,t,s])=>`<a class="resource" href="resources/${f}" ${f.endsWith('.pdf')?'target="_blank" rel="noopener"':'download'}><strong>${t}</strong><span>${s}</span></a>`).join('')}</div>
      <div class="notice"><strong>Filename check:</strong> download <code>pattern.txt</code>, but type <code>pattern</code> at the prompt. For <code>one.txt</code>, type <code>one</code>. The program adds <code>.txt</code> itself.</div>
      <details><summary>Additional copies: 0.txt and 1.txt</summary><p>These remain available for earlier activities. Use the official names above for today's tests.</p><a href="resources/0.txt" download>0.txt</a> · <a href="resources/1.txt" download>1.txt</a></details>${nav('starter')}`),
    page('starter','02','Starter','Find it. Explain it.','5 minutes · 3 marks',`
      <div class="notice">Use the unmodified skeleton. Locate the evidence before answering. Your teacher will check the answers before programming begins.</div>${official('starter-q01-1-3.png','Q01.1–Q01.3')}
      <div class="two-col">${field('q01_1','01.1 Answer',1)}${field('q01_2','01.2 Answer',1)}</div>${field('q01_3','01.3 Answer',2)}
      <details><summary>If you need a reminder: code vocabulary</summary><p>A <strong>parameter</strong> is a name in a subroutine definition that receives a value when called. A <strong>local variable</strong> belongs to a subroutine. <strong>Concatenation</strong> joins strings. Locate each idea in the skeleton.</p></details>${field('starter_evidence','Point to one line that supports your answer. What does it show?',2)}
      ${teacher('<strong>Correct answers:</strong> 01.1 createImage only; 01.2 row or col; 01.3 joining two or more strings. darkenImage and lightenImage each have one parameter. The official scheme assigns these selected questions to AO3. Keep the 3-mark total. Follow up a guessed answer by asking for its evidence.')}${nav('learning','starter')}`),
    page('learning','03','Types of learning','Choose how to get better','2 minutes',`
      <p class="lead">You may be developing all three types. Choose what you most need to strengthen today, then use its strategy in the programming task.</p>
      <div class="three-col"><section class="card"><p class="eyebrow">Knowledge · remember</p><h3>Know what the code means</h3><p>Parameters, returned values, conditions and conversion.</p><p><strong>Strategy:</strong> find an example in the skeleton and explain it.</p></section><section class="card"><p class="eyebrow">Skills · practise</p><h3>Make and test a change</h3><p>Locate the right subroutine, modify it and run a useful test.</p><p><strong>Strategy:</strong> change one thing, predict its effect, then test.</p></section><section class="card"><p class="eyebrow">Understanding · connect</p><h3>Explain why it works</h3><p>Connect statement order and changing values to behaviour.</p><p><strong>Strategy:</strong> trace the values and justify the result.</p></section></div>
      ${select('learning_type','My priority today',['Knowledge','Skills','Understanding'])}${field('learning_strategy','The strategy I will use, and where I will use it…',2,'Example: trace factor after each input to explain whether the loop repeats.')}${nav('main1','learning')}`),
    page('main1','04','Main Activity 1','Report the loaded dimensions','15 minutes · Q05 / 5',`
      <section class="reading card"><p class="eyebrow">Read before coding · lesson support</p><h3>New values arrive when the load finishes</h3><p><code>loadImage()</code> asks for a filename, reads dimensions and pixel data, and returns three values in order: width, height and the image list. In <code>main</code>, these are assigned to <code>width</code>, <code>height</code> and <code>image</code>.</p><p>Output uses the values stored when it executes. At the start of <code>main</code>, width and height are zero. A message before the loading assignment would use those initial values on the first load, or old dimensions after a previous load.</p><p><code>pattern.txt</code> starts with 5 and 9: width 5, height 9. Keep that order. The remaining lines contain pixel data.</p><p class="small">Teacher-created explanation, referencing the supplied skeleton (main, loadImage), pattern.txt and Q05, question-paper p. 6.</p></section>
      ${example('def loadCard():\n    return 4, 7\n\ncolumns = 0\nrows = 0\ncolumns, rows = loadCard()\nprint(f"Card: {columns} columns, {rows} rows")','practice_card.py')}
      ${field('q5_prediction','Predict the practice output. What changes if print moves immediately before the assignment from loadCard()?',2)}<details><summary>Check the practice after predicting</summary><p>It prints <code>Card: 4 columns, 7 rows</code>. Before the assignment it would print zero for both. Returned values must be assigned before they are used.</p></details>
      ${official('main1-q05.png','Q05 · 4 source marks + 1 evidence mark')}
      <section class="card"><p class="eyebrow">Checkpoint 1 · plan</p><h3>Locate the change</h3><div class="two-col">${field('q5_location','Where will your output go, and why?',2)}${field('q5_vars','Which variables must it use, and in which order?',2)}</div>${field('q5_expected','Predict the exact message for the official pattern test.',2)}</section>
      <details><summary>Support: output with integer values</summary><p><code>int(...)</code> converts the file's dimensions to integers. An f-string inserts their values into a message, as in the card example. If you join strings using <code>+</code>, convert integers with <code>str(...)</code> first.</p></details>
      ${editor('q05_code','05.1 · Paste ALL of your amended main subroutine')}${feedback('q05','q05_code')}
      <section class="card"><p class="eyebrow">Run the official test</p><p>Choose <code>1</code>, then type <code>pattern</code>. Check the whole dimensions message and its position after loading. Capture the result.</p>${field('q05_test','Official test: did the output match your prediction? Explain any difference.',2)}</section>${uploads('q05')}
      ${teacher('<strong>Q05:</strong> appropriate output position, uses width, uses height, exact message and correct order (4 source marks). Matching official test screenshot (1). Expected: The image has size 5 by 9 pixels. Ask why a rectangular image exposes reversed dimensions better than the square illustration. Revisit to see the retest.')}${nav('main2','main1')}`),
    page('main2','05','Main Activity 2','Validate, repeat, continue','25 minutes · Q06 / 6',`
      <section class="reading card"><p class="eyebrow">Read before coding · lesson support</p><h3>Keep asking while the value is invalid</h3><p><code>stretchImage</code> repeats pixels across each row. Its <code>factor</code> controls the new width. Q06 asks you to validate this factor <strong>before</strong> the stretching code uses it.</p><p><code>input()</code> returns a string; <code>int()</code> converts it to an integer. Validation checks whether that integer satisfies a rule. A <code>while</code> loop checks a Boolean condition before each iteration: when true, its indented block runs; when false, execution continues below it.</p><p>For invalid input, display the required message and request another value <strong>inside the loop</strong>. Without a new assignment to factor, the same condition keeps giving the same result. Keep stretching calculations after validation.</p><p class="small">Teacher-created explanation, referencing stretchImage and Q06, question-paper p. 7. The official rule is “less than 6”; extra restrictions are not part of this question.</p></section>
      <details><summary>Worked practice: follow the changing value</summary><p>A practice ticket machine accepts an integer below 4. Predict the path for inputs 4, then 2 before running it.</p>${example('tickets = int(input("Number of tickets: "))\nwhile tickets >= 4:\n    print("Choose fewer than 4 tickets")\n    tickets = int(input("Number of tickets: "))\nprint("Continue booking")','practice_tickets.py')}<p>With 4, the condition is true: display the message and read again. With 2, it is false: leave the loop and continue. Apply Q06's own requirement below.</p></details>
      ${official('main2-q06.png','Q06 · 5 source marks + 1 evidence mark')}
      <section class="card"><p class="eyebrow">Checkpoint 1 · before coding</p><h3>Decide the boundary</h3><p>Answer individually. You can first show your decisions on a mini-whiteboard, then record them here.</p><div class="three-col">${[5,6,7].map(n=>select('q6_boundary_'+n,'Factor '+n,['Accept','Reject'])).join('')}</div>${field('q6_condition','Write an invalid condition using factor. Explain how it matches the rule.',2)}<button class="button" id="check-boundary">Check my predictions</button><p id="boundary-feedback" role="status"></p><p class="small">First submitted predictions are kept. Correct selections still need a reasoned explanation.</p></section>
      <section class="card"><p class="eyebrow">Checkpoint 2 · during coding</p><h3>Will it reject twice?</h3><p>Trace your draft for <strong>6, 7, 3</strong>. Type values, or draw a paper trace and photograph it for the evidence area.</p><div class="table-scroll"><table><thead><tr><th>Input assigned to factor</th><th>Your condition: true / false?</th><th>What happens next?</th></tr></thead><tbody>${[6,7,3].map((n,i)=>`<tr><th>${n}</th><td><input aria-label="Condition for ${n}" data-save="q6_trace_${i}_condition" /></td><td><input aria-label="Next action for ${n}" data-save="q6_trace_${i}_action" /></td></tr>`).join('')}</tbody></table></div>${field('q6_reinput','Which statement changes factor? Why must it be inside the loop?',2)}</section>
      <details><summary>Hints: choose the smallest help you need</summary><p><strong>1. Locate:</strong> after the first input, before <code>newWidth</code> is calculated.</p><details><summary>2. Repetition</summary><p>An <code>if</code> checks once. The number of invalid attempts is unknown; use condition-controlled repetition.</p></details><details><summary>3. Boundary</summary><p>The largest permitted integer under this rule is 5. Equality with 6 must be rejected. Check your comparison against the table.</p></details><details><summary>4. Termination</summary><p>Find the first assignment that reads and converts factor. The repeating block needs another assignment with that role.</p></details></details>
      ${editor('q06_code','06.1 · Paste ALL of your amended stretchImage subroutine')}${feedback('q06','q06_code')}
      <section class="card"><p class="eyebrow">Checkpoint 3 · run and retest</p><h3>Use tests that can expose a fault</h3><p>For each separate run, load <code>one</code> using option <code>1</code>, then choose option <code>4</code>. Record actual behaviour.</p><div class="table-scroll"><table><thead><tr><th>Test</th><th>Expected behaviour</th><th>Your actual result</th></tr></thead><tbody><tr><th>5<br /><small>Additional</small></th><td>Proceed without a validation error.</td><td><textarea rows="3" aria-label="Actual result for 5" data-save="q06_test5"></textarea></td></tr><tr><th>6, then 3<br /><small>Official</small></th><td>Show the specified error for 6; ask again; proceed after 3.</td><td><textarea rows="3" aria-label="Official test actual result" data-save="q06_test63"></textarea></td></tr><tr><th>6, 7, then 3<br /><small>Additional</small></th><td>Reject twice, then proceed. This can expose a one-check solution.</td><td><textarea rows="3" aria-label="Repeated invalid input result" data-save="q06_test673"></textarea></td></tr></tbody></table></div><p>Use <code>Invalid input - please try again</code>. Capture the specified 6-then-3 run for 06.2. Additional tests supplement it.</p></section>${uploads('q06')}
      ${teacher('<strong>Q06:</strong> indefinite iteration; test of factor; correct invalid criterion; message inside iteration; another input when invalid (5 source marks; maximum 4 if errors). Integer criteria factor &gt; 5 and factor &gt;= 6 are equivalent. Matching official screenshot (1). Do not impose positivity or exception handling as official requirements. Ask “What changes before the condition is checked again?” Revisit to inspect the retest.')}${nav('pitstop','main2')}`),
    page('pitstop','06','Learning pit stop','Where are you now?','3 minutes',`
      <p class="lead">Choose your current phase. Support it with a code line or test result, then decide your next action. This is a temporary phase, not a label for you.</p><div class="two-col"><section class="card"><p class="eyebrow">New learning</p><h3>A productive struggle</h3><p>I can explain repeated validation, but still need the trace.</p><p><strong>Next:</strong> try a new sequence with less support.</p></section><section class="card"><p class="eyebrow">Consolidating</p><h3>Using what I know</h3><p>My tests pass and I can explain the condition and re-input.</p><p><strong>Next:</strong> apply the idea independently in the plenary.</p></section><section class="card"><p class="eyebrow">Treading water</p><h3>Ready for more challenge</h3><p>I completed this independently and can justify the tests.</p><p><strong>Next:</strong> choose a deeper extension while others finish.</p></section><section class="card"><p class="eyebrow">Drowning</p><h3>I need help to move forward</h3><p>I cannot explain why the condition stays true, or how to fix my program.</p><p><strong>Next:</strong> show the exact confusion and trace one iteration with the teacher.</p></section></div>
      ${select('pitstop_phase','My current phase',['New learning','Consolidating','Treading water','Drowning'])}${field('pitstop_evidence','The line or test result supporting my choice…',2)}${field('pitstop_action','My next action to improve this program or deepen my understanding…',2)}${nav('plenary','pitstop')}<p class="small">Ready early? Open Extension. Everyone returns for the plenary.</p>`),
    page('extension','07','Extension','Test more deeply','Optional · Q07 / 9',`
      <section class="card"><p class="eyebrow">Short route · teacher-created practice</p><h3>Find the test that catches the fault</h3><p>A programmer replaces your validation loop with one <code>if</code>, keeping the message and one new input inside it. It appears to work for 6 then 3.</p><ol><li>Choose a sequence that exposes its weakness.</li><li>Predict how this differs from a correct loop.</li><li>Explain why one successful test does not prove repeated validation works.</li></ol>${field('extension_sequence','My sequence, expected behaviour and explanation…',4)}<details><summary>If you already explained 6, 7, 3 during Q06</summary><p>Consider <code>0</code>, a negative integer and <code>hello</code>. Which issue is conversion failure, and which values satisfy “less than 6”? Explain why an application might need stronger requirements. Propose those separately; do not change the official Q06 submission.</p></details></section>
      <section class="card"><p class="eyebrow">Longer route · original examination task</p><h3>Q07: generate a random image</h3><p>Use a fresh skeleton. Create a 5-by-6 image whose 30 pixels are random integers from 0 to 4 inclusive, while preserving normal file loading.</p><p><code>createImage(width, height)</code> creates zero-valued pixels. List indices start at zero. <code>random.randint(a, b)</code> includes both endpoints. Assign each generated value to its pixel position.</p></section>
      ${official('extension-q07.png','Q07 · 8 source marks + 1 evidence mark')}${field('q7_plan','Plan the filename branch, size, positions, assignment and returned values.',4)}${editor('q07_code','07.1 · Paste ALL of your amended loadImage subroutine')}${field('q7_tests','Run the official 1 → random → 2 test, then check ordinary loading with pattern. What does each test establish?',3)}${uploads('q07')}
      ${teacher('<strong>Q07:</strong> 8 source marks, maximum 7 if errors, plus 1 evidence mark. Check filename selection, dimensions, all 30 positions, inclusive 0–4, pixel updates and preserved ordinary loading. The random image need not match the illustration. Do not delay the plenary for this optional task.')}${nav('plenary','extension')}`),
    page('plenary','08','Plenary','Can you transfer the idea?','5 minutes · independent',`
      <section class="card"><p class="eyebrow">Teacher-created exit task</p><h3>A booking program with two faults</h3><p>This separate program accepts an integer <strong>below 10</strong>. It should reject invalid integers, ask again and repeat until valid. Assume users enter integers.</p><p>Work independently: identify both faults, correct the block, and choose tests that expose them.</p>${example('places = int(input("Enter places: "))\nwhile places > 10:\n    print("Invalid")\nprint("Booking continues")','exit_booking.py')}${field('exit_reason','Identify both faults and explain their effects.',3)}${field('exit_statement','Write the corrected Python block, including its initial input.',6)}${field('exit_tests','Choose tests that expose each fault. What should the corrected program do?',3)}<p class="small">Need help? Record what you can explain and where you are stuck. You can still export.</p></section>
      ${teacher('<strong>Exit:</strong> reject equality with 10; read a fresh integer into places inside the loop. Accept equivalent valid structures. Input 10 exposes the boundary; 11 exposes the unchanged-value loop. Corrected code should reject 10 then accept 2, and reject 11 then accept 2. This is independent learning evidence, not an official question.')}${nav('finish','plenary')}`),
    page('finish','09','Finish & export','Keep the evidence of learning','5 minutes',`
      <section class="card"><h3>Check your record before submitting</h3><p>Your PDF includes responses, current code, saved attempts, feedback, retests, reflections and evidence. Missing responses are labelled so unfinished work can still be reviewed.</p><div id="completion-list"></div><p class="small">“Recorded” means you marked an activity as recorded. It does not verify correctness or award marks.</p></section><section class="card dark"><h3>Download, check, submit</h3><ol><li>Download your PDF.</li><li>Open it: check your name, indentation and screenshot readability.</li><li>Submit it to the correct Teams assignment.</li></ol><button id="export-pdf" class="button inverse">Download learning record PDF</button></section><div class="notice">A partial record is useful evidence. Include where you need help; a completed screenshot is not required to export.</div><button id="clear-work" class="button">Clear this student's saved work</button>`)
  ];
  document.querySelector('#pages').innerHTML = lessons.map(x=>x.html).join('');
  document.querySelector('#lesson-nav').innerHTML = lessons.map((x,i)=>`<button data-nav="${x.id}"><span>${String(i).padStart(2,'0')}</span>${x.label}</button>`).join('') + '<button data-nav="teacher" class="teacher-only" hidden><span>T</span>Teacher notebook</button>';
  window.lessonConfig = {lessons:lessons.map(({id,label})=>({id,label})), escape};
})();
