/* Slides are discoverable only in the teacher panel; students receive a slide through Classroom Mode. */
(()=>{'use strict';const plan=window.SESSION_PLAN,clock=window.LessonClock;
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const facts=[{"id": "fact-loop", "title": "Fact 1 · Four values mean four repetitions", "body": "<pre><code>for i in range(4):\n    t.forward(80)\n    t.right(90)</code></pre><table><tr><th>i takes</th><td>0</td><td>1</td><td>2</td><td>3</td></tr><tr><th>Repetition</th><td>1st</td><td>2nd</td><td>3rd</td><td>4th</td></tr></table><p>Each value runs the indented move–turn pair once. There are four moves and four turns. The stopping value 4 is not included.</p><div class=\"tp-question\"><b>Discuss</b><p>Why does starting at 0 still give four repetitions? How many times does forward run?</p></div>", "zh": "range(4) 给出 0、1、2、3，共四个值。每次执行缩进的前进与转弯。讨论：为什么从 0 开始仍重复四次？forward 执行多少次？"}, {"id": "fact-indent", "title": "Fact 2 · Indentation changes what repeats", "body": "<div class=\"tp-compare\"><section><h2>Turn inside</h2><pre><code>for i in range(4):\n    t.forward(80)\n    t.right(90)</code></pre><p>Move and turn four times: a square.</p></section><section><h2>Turn outside</h2><pre><code>for i in range(4):\n    t.forward(80)\nt.right(90)</code></pre><p>Move four times in one direction, then turn once.</p></section></div><div class=\"tp-question\"><b>Discuss</b><p>In the second program, which command runs only once? How would you change it to draw a square?</p></div>", "zh": "缩进决定哪些指令属于循环。左边前进与转弯都重复四次；右边前进四次，最后只转一次。讨论：第二段程序中哪条指令只执行一次？怎样修改才能画正方形？"}, {"id": "fact-debug", "title": "Fact 3 · No error message does not mean correct", "body": "<table><tr><th>Problem</th><th>What happens?</th><th>Example</th></tr><tr><td>Syntax</td><td>Python cannot read the code.</td><td>A missing colon after the loop header.</td></tr><tr><td>Runtime</td><td>Execution starts, then stops.</td><td>A name is used without a value.</td></tr><tr><td>Logic</td><td>Code runs, but misses the target.</td><td>Three sides when the goal is a square.</td></tr></table><pre><code>for i in range(3):\n    t.forward(60)\n    t.right(90)</code></pre><div class=\"tp-question\"><b>Discuss</b><p>The target is a complete square. What kind of error is this? What one change would you make, and what would you check after running again?</p></div>", "zh": "语法错误：Python 无法读懂代码。运行时错误：开始执行后停止。逻辑错误：能运行，但结果不符合目标。讨论：目标是完整正方形，这是什么错误？改哪一处？再次运行后检查什么？"}];
const slides=[];for(const s of plan.stages){slides.push({...s,body:`<ol>${s.steps.map(t=>`<li>${esc(t)}</li>`).join('')}</ol>${s.id==='types'?'<blockquote>Amazing Learning happens when we practice, try hard and use good struggle to get better.</blockquote>':s.id==='pitstop'?'<p class="tp-phase">New learning · Consolidating · Treading water · Drowning</p>':''}`});if(s.id==='loop')slides.push(facts[0]);if(s.id==='indent')slides.push(facts[1]);if(s.id==='errors')slides.push(facts[2]);}
slides.splice(slides.findIndex(s=>s.id==='pitstop'),0,...[{"id": "extension", "title": "Extra time · Draw something of your own", "body": "<ol><li>Choose a star or house. Read the shape hints.</li><li>Use a loop, then change the colour or size.</li><li>Run and compare. Explain one choice.</li></ol>", "zh": "有时间时，选择星形或房子，用循环绘图，改变颜色或大小，运行并解释一项选择。"}, {"id": "poster", "title": "Extra time · Teach someone with a poster", "body": "<ol><li>Add a title and three Turtle commands with meanings.</li><li>Include a small loop and a useful debugging tip.</li><li>Preview your poster and ask your teacher for feedback.</li></ol>", "zh": "制作海报：标题、三个指令及含义、小循环、调试建议。预览后请老师提供反馈。"}]);
const isTeacher=()=>window.LessonClassroom.info().teacher;
const questions={"fact-loop": "Why does starting at 0 still give four repetitions? How many times does forward run?", "fact-indent": "In the second program, which command runs only once? How would you change it to draw a square?", "fact-debug": "The target is a complete square. What kind of error is this? What one change would you make, and what would you check after running again?"};
const teacherAnswers={};try{Object.assign(teacherAnswers,JSON.parse(localStorage.getItem('y7w5-teacher-discussion')||'{}'));}catch{}
let index=0,dialog=null,tick=null,returnFocus=null;
const blocked=()=>!isTeacher()&&!!window.ClassroomMode?.locked();
function answerBox(s){return questions[s.id]?`<div class="tp-answer"><label for="tp-answer">Your discussion answer</label><textarea id="tp-answer" rows="4" placeholder="Write your explanation here.">${esc(isTeacher()?(teacherAnswers[s.id]||''):window.LessonClassroom.answer(s.id))}</textarea><small>Saved on this device. ${isTeacher()?'Your answer is not sent to students.':'Included in your full backup and PDF.'}</small></div>`:'';}

function draw(){const s=slides[index];dialog.innerHTML=`<div class="tp-toolbar"><b>${isTeacher()?'TEACHER PRESENTATION':'CLASS DISCUSSION'}</b>${isTeacher()?'<button id="tp-send" class="primary">Bring everyone to this slide</button>':''}<span id="tp-clock"></span><button id="tp-close" ${blocked()?'disabled':''}>${isTeacher()?'Close presentation':'Return to lesson'}</button></div><div class="tp-layout"><aside><section><h2>Topic</h2><p>${esc(plan.topic)}</p><small>Year 7 · Week 5 · Theory<br>Repeat, check and fix</small></section><section><h2>WAGBA</h2><p>${esc(plan.wagba)}</p></section><section><h2>Knowledge · Skills · Understanding</h2><p>Identify loops and error types.<br>Predict, run, change and test.<br>Explain why the drawing matches the goal.</p></section><section><h2>Keywords</h2><p>${esc(plan.keywords)}</p></section><section><h2>Challenge</h2><p>${esc(plan.challenge)}</p></section></aside><main><p class="tp-eyebrow">${s.start?clock.time(s.start)+'–'+clock.time(s.end)+' · '+(s.end-s.start)+' minutes':'Teacher-led discussion · within task time'}</p><h1 id="tp-title" tabindex="-1">${esc(s.title)}</h1><div class="tp-content">${s.body}${window.LessonClassroom.info().lang==='zh'&&s.zh?`<p class="tp-translation" lang="zh">${esc(s.zh)}</p>`:''}${answerBox(s)}</div><p class="tp-note">${isTeacher()?'Choose a slide, then click Bring everyone to this slide. Close the presentation to bring students to a normal lesson page.':'Your teacher selected this slide. Answer in your own words; your teacher’s answer is not copied to your device.'}</p></main></div><p id="tp-send-status" role="status"></p><footer ${isTeacher()?'':'hidden'}><button id="tp-prev" ${index===0?'disabled':''}>← Previous</button><label>Slide <select id="tp-select">${slides.map((s,i)=>`<option value="${i}" ${i===index?'selected':''}>${i+1}. ${esc(s.title)}</option>`).join('')}</select></label><span>${index+1} / ${slides.length}</span><button id="tp-next" ${index===slides.length-1?'disabled':''}>Next →</button></footer>`;
dialog.querySelector('#tp-close').onclick=()=>{if(!blocked())dialog.close();};
const input=dialog.querySelector('#tp-answer');if(input)input.oninput=()=>{if(isTeacher()){teacherAnswers[s.id]=input.value;try{localStorage.setItem('y7w5-teacher-discussion',JSON.stringify(teacherAnswers));}catch{}}else window.LessonClassroom.setAnswer(s.id,input.value);};
const send=dialog.querySelector('#tp-send');if(send)send.onclick=async()=>{send.disabled=true;const status=dialog.querySelector('#tp-send-status');status.textContent='Sending…';const ok=await window.ClassroomMode?.sendSlide('slide-'+s.id);status.textContent=ok?'Students are being brought to this slide.':'Not sent. Start a classroom first, or check the classroom panel for the error.';send.disabled=false;};dialog.querySelector('#tp-prev').onclick=()=>choose(index-1);dialog.querySelector('#tp-next').onclick=()=>choose(index+1);dialog.querySelector('#tp-select').onchange=e=>choose(+e.target.value);update();}
function choose(i){index=Math.max(0,Math.min(slides.length-1,i));draw();dialog.scrollTop=0;dialog.querySelector('#tp-title').focus({preventScroll:true});}
function update(){if(!dialog?.open)return;dialog.querySelector('#tp-close').disabled=blocked();const x=clock.at(clock.seconds()),n=Math.max(0,Math.ceil(x.remaining));dialog.querySelector('#tp-clock').textContent=x.phase==='before'?'Starts 1:00 p.m.':x.phase==='after'?'2:00 p.m. · lesson time ended':x.stage.title+' · '+String(Math.floor(n/60)).padStart(2,'0')+':'+String(n%60).padStart(2,'0')+' remaining';}
window.TeacherPresentation=Object.freeze({
 ids:()=>slides.map(s=>'slide-'+s.id), questions,
 current:()=>dialog?.open?'slide-'+slides[index].id:null,
 title:()=>slides[index].title,
 isOpen:()=>!!dialog?.open,
 close(){dialog?.close();},
 open(id){if(!isTeacher()&&!id)return;if(id){const i=slides.findIndex(s=>'slide-'+s.id===id);if(i<0)return;index=i;}
 returnFocus=document.activeElement;
 if(!dialog){dialog=document.createElement('dialog');dialog.id='teacher-presentation';dialog.setAttribute('aria-label','Lesson presentation');document.body.append(dialog);
 dialog.addEventListener('cancel',e=>{if(blocked())e.preventDefault();});
 dialog.addEventListener('close',()=>{clearInterval(tick);window.dispatchEvent(new Event('lesson:render'));returnFocus?.focus();});}
 draw();if(!dialog.open)dialog.showModal();dialog.scrollTop=0;update();clearInterval(tick);tick=setInterval(update,1000);window.dispatchEvent(new Event('lesson:render'));
 }
});
})();
