/* Lesson content is separate from presentation and saved student work. */
(()=>{
const goals={topic:'Selection and comparison operators',wagba:'We are getting better at using comparisons to predict and explain which instructions a program will run.',K:'I can explain conditions, six comparison operators and the purpose of if / else.',S:'I can predict a branch, write indented selection and test both outcomes.',U:'I can explain how changing a value or comparison changes the program path.'};
const operators=[['==','Equal to'],['!=','Not equal to'],['>','Greater than'],['<','Less than'],['>=','Greater than or equal to'],['<=','Less than or equal to']];
const stages=[['read','Read first','4 min'],['starter','Do Now','6 min'],['types','Types of Learning','4 min'],['operators','Operator reference','3 min'],['main1','Main Task 1','10 min'],['syntax','Read the syntax','3 min'],['main2','Main Task 2','16 min'],['extension','Extension','If time'],['pit','Learning Pit Stop','4 min'],['plenary','Plenary','5 min'],['review','Review & PDF','5 min']];
const cards=[];
function add(id,stage,title,type,data={}){cards.push({id,stage,title,type,core:true,...data});}
const q=(label,options,answer,why)=>({label,options,answer,why});
const code=(score=6,op='>=',target=5)=>`score = ${score}\nif score ${op} ${target}:\n    print("READY")\nelse:\n    print("KEEP GOING")`;
const operatorTable='<table><thead><tr><th>Symbol</th><th>Meaning</th><th>With score = 5</th></tr></thead><tbody>'+operators.map(([s,m])=>`<tr><td><code>${s.replaceAll('<','&lt;').replaceAll('>','&gt;')}</code></td><td>${m}</td><td><code>score ${s.replaceAll('<','&lt;').replaceAll('>','&gt;')} 5</code></td></tr>`).join('')+'</tbody></table>';
add('mission','read','How can a badge make a choice?','reading',{body:'At our school welcome activity, students collect points for completing challenges. A Smart Badge should display READY when a student has at least five points. Otherwise, it should display KEEP GOING. A comparison asks whether a rule is true. Selection uses that answer to choose a path. Today you will read comparisons, predict outputs, arrange code and build short Python programs. You will test at the boundary, not just guess.',html:'<div class="rule"><span>5 or more points <b>READY</b></span><span>Fewer than 5 <b>KEEP GOING</b></span></div><p>This is a screen-based Python model. No micro:bit connection is needed.</p><table><tr><th>Comparison</th><th>What it asks</th></tr><tr><td><code>score &gt;= 5</code></td><td>Is the score at least five?</td></tr><tr><td><code>score == 5</code></td><td>Is the score exactly five?</td></tr></table>',questions:[q('What does a comparison produce?',['A True or False result','A new score every time','Both program paths'],0,'A comparison checks a relationship. It does not automatically change the value.')],support:['先阅读规则。比较的结果是 True（真）或 False（假）。今天不用连接 micro:bit。','규칙을 먼저 읽으세요. 비교의 결과는 True(참) 또는 False(거짓)입니다. 오늘은 micro:bit 연결이 필요 없습니다.']});
const starters=[
 ['A value and a comparison','score = 4\nprint(score >= 5)',q('What is displayed?',['True','False','4'],1,'Four is below five, so the comparison is False.')],
 ['Exactly at the boundary',code(5),q('Which message appears?',['READY','KEEP GOING','Both messages'],0,'>= includes equality: five is enough.')],
 ['Assignment or comparison?','score = 5\nprint(score == 5)',q('Which statement is accurate?',['Both lines change score','The first assigns; the second compares and prints','== means add two values'],1,'= assigns a value. == compares two values.')],
 ['Which branch runs?',code(2),q('Which instructions run after the condition?',['Both branches','Only the if branch','Only the else branch'],2,'Two does not reach five, so the else branch runs.')],
 ['Beyond the branch',code(7)+'\nprint("DONE")',q('What is displayed in order?',['READY, then DONE','READY only','KEEP GOING, then DONE'],0,'DONE is outside the indented branches, so it runs afterwards.')]
];
starters.forEach(([title,sample,question],i)=>add('starter'+(i+1),'starter',title,'quiz',{code:sample,questions:[question],core:i<2,body:'Read the code. Predict before checking. These are starting-point questions, not a grade.'}));
function parson(id,stage,title,lines,why,core=false){add(id,stage,title,'parsons',{lines,why,core,body:'Put the lines into a working order using the up/down buttons. Spaces at the start of each line are already included; keep them. Read the finished program from top to bottom.'});}
parson('sp1','starter','Arrange the badge check',['score = 5','if score >= 5:','    print("READY")','else:','    print("KEEP GOING")'],'Assign the score before comparing it. The indented output belongs to its branch.',true);
parson('sp2','starter','Arrange a comparison',['score = 3','ready = score >= 5','print(ready)'],'The comparison uses score, then stores its Boolean result in ready.');
parson('sp3','starter','Keep the final message outside',['score = 2','if score < 5:','    print("PRACTISE")','else:','    print("READY")','print("DONE")'],'DONE is not indented and belongs after the two branches.');
parson('sp4','starter','Compare the team name',['team = "GREEN"','if team == "GREEN":','    print("GREEN DESK")','else:','    print("WELCOME DESK")'],'Store the team name before checking whether it equals GREEN.');
parson('sp5','starter','Exactly five points',['score = 5','if score == 5:','    print("EXACTLY FIVE")','else:','    print("ANOTHER SCORE")'],'== checks equality. The else branch covers all other values.');
add('before','types','What will getting better look like?','reflection',{phase:'before',body:'Learning means a change in what you know, can do or can explain. Knowledge is remembering ideas; skills improve through practice; understanding is explaining why and applying ideas. Choose honestly for each target. New learning is expected today.'});
add('six','operators','Meet the six comparisons','reading',{body:'Read each operator aloud. With score = 5, >= and == are True, but > is False. A comparison checks a value; it does not assign a new one. The result has a Boolean data type: True or False. You can return to this reference at any time.',html:operatorTable,questions:[q('Which operator means “not equal to”?',['==','!=','<='],1,'The exclamation mark in != changes the equality test to “not equal”.')],support:['把运算符读出来。= 是赋值，== 是比较是否相等。','연산자를 말로 읽어 보세요. =는 값을 대입하고, ==는 같은지 비교합니다.']});
add('match','main1','Label each comparison','match',{body:'Choose the correct meaning for all six symbols. Use the reference if needed, then try to explain one without looking.'});
const conditions=[['score = 5','score >= 5',true],['score = 5','score > 5',false],['score = 5','score == 5',true],['score = 3','score < 5',true],['score = 8','score <= 5',false],['score = 4','score != 5',true],['score = 5','score <= 5',true],['score = 6','score == 5',false]];
conditions.forEach(([assignment,test,answer],i)=>add('condition'+i,'main1',i<3?'At the boundary: '+test:'Try another condition: '+test,'quiz',{core:i<3,body:'Substitute the stored value into the comparison. Does the statement become True or False?',code:assignment,questions:[q(test,['True','False'],answer?0:1,`${assignment}. The statement ${test} is ${answer?'True':'False'}. Read the operator’s meaning before trying again.`)]}));
add('trace-model','main1','Follow one path, line by line','trace',{body:'Selection chooses a branch. In this model, the condition is checked once. If True, the first indented print runs; otherwise, the print under else runs. Only one branch runs. Use “Next instruction” to see why.',code:code(6),support:['先找变量的值，再判断条件真假，最后只跟随所选分支。','변수 값을 확인하고 조건의 참·거짓을 판단한 다음, 선택된 분기만 따라가세요.']});
[4,5,8].forEach((v,i)=>add('path'+i,'main1',`Predict the path: score ${v}`,'quiz',{core:i===0,body:'Do not run this yet. Give the condition result, branch and output as one connected answer.',code:code(v),questions:[q('Which prediction is correct?',['True → if → READY','False → else → KEEP GOING','True → both branches → both messages'],v>=5?0:1,'Evaluate score >= 5 first. The result selects one branch.')] }));
add('output1','main1','Write the output for this rule','text',{body:'The badge displays READY for at least five points. With score = 3, write only the message it should display.',fields:['Expected message'],answers:['KEEP GOING'],prompts:['Use one of the two messages from the mission.'],why:'Three is below five, so the message is KEEP GOING.',core:false});
add('output2','main1','Explain a different comparison','text',{body:'The badge displays SPECIAL only when score is exactly 5. It displays NORMAL otherwise. Predict the message for score = 6, then explain the comparison you would use.',fields:['Expected message','My explanation'],answers:['NORMAL',null],prompts:['Write the message.','I would use ___ because “exactly” means ___.'],why:'Six is not exactly five, so the message is NORMAL. The comparison for exactly five is score == 5.',core:false});
add('syntax-read','syntax','Colons, branches and indentation','reading',{body:'Write a colon after the if condition and after else. Indent the instructions inside each branch by four spaces. Align else with if. An unindented instruction after the structure runs after either branch. Python uses this layout to group instructions; indentation is not just decoration.',code:code(6)+'\nprint("THANK YOU")',html:'<p><b>Our editor runs ordinary Python.</b> The micro:bit editors are reference links only: <a href="https://makecode.microbit.org/" target="_blank" rel="noopener noreferrer">MakeCode (Blocks / MakeCode Python)</a> · <a href="https://python.microbit.org/" target="_blank" rel="noopener noreferrer">micro:bit MicroPython</a>. Their device display commands differ. Do your challenges in the embedded editor below.</p>',questions:[q('Where does the colon go?',['After the condition and after else','Before the variable name','Only after print'],0,'Use if condition: and else: to introduce the two blocks.'),q('What does indentation show?',['The instructions belonging to a branch','The score is larger','A comment'],0,'Indented instructions form a block controlled by that branch.'),q('When does the final unindented THANK YOU run?',['After either branch','Only when the condition is True','Never'],0,'The final line is outside both branches.')],support:['if 条件和 else 后要有冒号。分支内缩进四个空格。','if 조건과 else 뒤에는 콜론을 씁니다. 분기 안의 코드는 네 칸 들여씁니다.']});
parson('mp1','main2','Build the if / else structure',['score = 6','if score >= 5:','    print("READY")','else:','    print("KEEP GOING")'],'if and else align. Each output is indented inside its own branch.',true);
parson('mp2','main2','An exact-score award',['score = 8','if score == 8:','    print("EXACT AWARD")','else:','    print("STANDARD")'],'The equality condition belongs before its True branch.');
parson('mp3','main2','Finish both paths politely',['score = 1','if score >= 5:','    print("READY")','else:','    print("KEEP GOING")','print("THANK YOU")'],'The final unindented print follows either branch.');
add('indent1','main2','Which output is inside the branch?','quiz',{core:false,code:'score = 2\nif score >= 5:\n    print("READY")\nelse:\n    print("KEEP GOING")\n    print("TRY AGAIN")\nprint("DONE")',questions:[q('Select the complete output in order.',['KEEP GOING / TRY AGAIN / DONE','KEEP GOING / DONE','READY / DONE'],0,'Both indented lines under else run, followed by the unindented DONE.')]});
add('indent2','main2','One line changes position','quiz',{core:false,code:'score = 8\nif score >= 5:\n    print("READY")\nelse:\n    print("KEEP GOING")\nprint("TRY AGAIN")',questions:[q('What is displayed?',['READY only','READY / TRY AGAIN','KEEP GOING / TRY AGAIN'],1,'TRY AGAIN is outside both branches. Indentation changes meaning, even if the program has no syntax error.')]});
function ide(id,title,body,starter,tests,extra={}){add(id,'main2',title,'ide',{body,starter,tests,fields:['My prediction or test plan','What I changed / what I learned'],prompts:['For ___ I expect ___ because ___.','I changed ___; my test with ___ showed ___.'],...extra});}
ide('debug1','Repair the boundary','A trainee used >, but the badge must say READY for five or more points. Repair only the condition. Keep score = 5. Run first, then use “Test examples” to check scores 4, 5 and 6.',code(5,'>'),[{score:4,expect:'KEEP GOING\n'},{score:5,expect:'READY\n'},{score:6,expect:'READY\n'}],{hint:'The rule includes five itself. Which comparison means “greater than or equal to”?'});
ide('debug2','Repair a syntax error','This program should display the correct badge message. Find the missing punctuation and indentation. The compiler feedback will help you locate the first problem. Fix one thing at a time.', 'score = 3\nif score >= 5\nprint("READY")\nelse:\nprint("KEEP GOING")',[{score:3,expect:'KEEP GOING\n'},{score:5,expect:'READY\n'}],{core:false,hint:'Put a colon after the if condition. Indent each print inside its branch by four spaces.'});
parson('pp1','main2','Plan a console conversation',['name = input("Initials: ")','if name == "AB":','    print("WELCOME AB")','else:','    print("WELCOME VISITOR")'],'input() waits in the console and returns typed text. Compare that text only after it has been entered.');
parson('pp2','main2','Plan an entered score',['score = int(input("Score: "))','if score >= 5:','    print("READY")','else:','    print("KEEP GOING")'],'input() returns text. The supplied int(...) converts whole-number text into a number before comparing.');
ide('code1','Your badge, your boundary','Build a badge model that displays READY at five points or more, otherwise KEEP GOING. Keep the starting score line. Complete the if / else yourself. Test below, at and above five.', 'score = 5\n# Write your if / else below.\n',[{score:4,expect:'KEEP GOING\n'},{score:5,expect:'READY\n'},{score:6,expect:'READY\n'}],{core:false,hint:'Use if score >= 5: followed by an indented print. Add else: aligned with if.'});
ide('code2','Let the visitor type','A visitor types their initials. If they type AB, display WELCOME AB; otherwise display WELCOME VISITOR. The input line is provided. Run, click after “Initials:” in the console, type your answer and press Enter.', 'name = input("Initials: ")\n# Compare name with "AB" and choose a message.\n',[{input:['AB'],expect:'WELCOME AB\n'},{input:['XY'],expect:'WELCOME VISITOR\n'}],{hint:'Use name == "AB". Input is text, so the initials in your comparison need quotes.',support:['点击运行后，在控制台提示后输入，再按 Enter。不要把答案填入其他框。','실행 후 콘솔의 질문 바로 뒤에 입력하고 Enter를 누르세요. 별도의 답안 칸에 쓰지 마세요.']});
ide('code3','An entered score','The badge now asks for a score. Complete the same five-point rule. We supply int(input(...)): input gives text and int converts whole-number text. Type 4, 5 and 6 in separate runs. Use whole numbers for this task.', 'score = int(input("Score: "))\n# Complete the five-point badge rule.\n',[{input:['4'],expect:'KEEP GOING\n'},{input:['5'],expect:'READY\n'},{input:['6'],expect:'READY\n'}],{core:false,hint:'The supplied first line collects and converts the score. Use the same comparison and two print messages as before.'});
const extensions=[
 ['ext1','Level 1 · A new target','The activity now needs at least eight points. Use console input. Display READY for eight or more, otherwise KEEP GOING. Test 7, 8 and 9.','score = int(input("Score: "))\n# Decide whether the new target is reached.\n',[{input:['7'],expect:'KEEP GOING\n'},{input:['8'],expect:'READY\n'},{input:['9'],expect:'READY\n'}],'Example run: Score: 8 → READY'],
 ['ext2','Level 2 · An exact award','Only a score of exactly eight gets SPECIAL. Every other score gets NORMAL. Use console input. Show that both seven and nine follow the other path.','score = int(input("Score: "))\n# Choose SPECIAL or NORMAL.\n',[{input:['7'],expect:'NORMAL\n'},{input:['8'],expect:'SPECIAL\n'},{input:['9'],expect:'NORMAL\n'}],'Example run: Score: 9 → NORMAL'],
 ['ext3','Level 3 · After either path','Use the original five-point rule. Then display THANK YOU after either result. Test below and at the target. Explain why the final line needs no indentation.','score = int(input("Score: "))\n# Add the badge decision, then the final message.\n',[{input:['4'],expect:'KEEP GOING\nTHANK YOU\n'},{input:['5'],expect:'READY\nTHANK YOU\n'}],'Example run: Score: 5 → READY, then THANK YOU']
];
extensions.forEach(([id,title,body,starter,tests,sample])=>ide(id,title,body,starter,tests,{stage:'extension',core:false,html:`<p><b>Prerequisites:</b> read a comparison; use if / else; type a whole number into the console.</p><p class="sample">${sample}</p>`,hint:'Compare your program with the syntax reference. Test the exact boundary as well as a value on either side.'}));
add('after','pit','Where am I in my learning?','reflection',{phase:'after',body:'Use your work, not just a feeling. Revisit each learning target, compare with your starting point and choose the phase that describes this part of your learning. You may be in a different phase for knowledge, skills and understanding.'});
add('exit-operators','plenary','Six symbols, six meanings','match',{body:'Try without the reference first. Match each operator to its meaning. Feedback helps you choose what to practise next.'});
add('exit-path','plenary','Explain one last program path','text',{body:'A badge needs at least eight points. Read the program and answer independently before checking.',code:code(7,'>=',8),fields:['Which message appears?','Why does this branch run?','What changes when score becomes 8?'],answers:['KEEP GOING',null,null],prompts:['Write the displayed message.','Because 7 ___ 8, the condition is ___, so ___.','At 8, the condition becomes ___ and ___.'],why:'Seven is below eight: the else branch displays KEEP GOING. At eight, the if branch displays READY. Explanations are saved for teacher review.'});
add('review','review','Keep the evidence of your learning','review',{body:'Review your first attempts, corrections, code and console transcripts. Export a PDF, check it, then upload it to the Teams assignment your teacher identifies for Week 5 Theory. This app does not submit to Teams.'});
const language={zh:{label:'中文支持',general:'先阅读情境，再预测、尝试和解释。可以用中文思考，再使用英文计算机术语。',frames:'因为条件是 ___，程序执行 ___ 分支，输出 ___。我测试了 ___，结果是 ___。',words:'selection 选择结构 · condition 条件 · comparison 比较 · branch 分支 · indentation 缩进 · boundary 临界值'},ko:{label:'한국어 도움',general:'상황을 읽고 예측한 뒤, 시도하고 설명하세요. 한국어로 생각한 다음 영어 컴퓨팅 용어를 사용해 보세요.',frames:'조건이 ___이므로 ___ 분기가 실행되어 ___이 출력됩니다. ___을 테스트했고 결과는 ___입니다.',words:'selection 선택 구조 · condition 조건 · comparison 비교 · branch 분기 · indentation 들여쓰기 · boundary 경계값'},ms:{label:'Sokongan Bahasa Melayu',general:'Baca situasi, ramalkan, cuba dan jelaskan. Gunakan istilah pengkomputeran bahasa Inggeris bersama bahasa pilihan anda.',frames:'Oleh sebab syarat itu ___, cabang ___ dijalankan dan memaparkan ___. Saya menguji ___ dan hasilnya ___.',words:'selection pemilihan · condition syarat · comparison perbandingan · branch cabang · indentation inden · boundary nilai sempadan'}};
window.LESSON={id:'y8-w5-selection-v1',title:'Choose the path',goals,operators,stages,cards,language,operatorTable};
})();
